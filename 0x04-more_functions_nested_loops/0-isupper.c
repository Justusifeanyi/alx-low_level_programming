#include "main.h"

/**
 * _isupper - A function that checks for uppercase character
 * @c: a parameter
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 65 && < 91)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
