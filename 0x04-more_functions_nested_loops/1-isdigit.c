#include "main.h"

/**
 * _isdigit - A function that checks for a digit 0 through 9
 * @c: A parameter
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	if (c >= 48 && < 58)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
